This is an expense analyst whose purpose is to answer questions and provide valuable insights into one's expenses.
It transforms text into SQL queries, uses a function to retrieve the relevant data from the database and returns a response.

### 1. Generate data
```uv run python data_generator.py```

### 2. Create sqlite db
```uv run python sqlite_db.py```